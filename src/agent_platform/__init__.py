"""agent_platform stub — minimal interface for Kachu pipeline tests.

This stub lives in the Kachu workspace so the pipeline-shape tests pass when
AgentOS is not installed.  Only the public surface required by the tests is
implemented; all execution logic is intentionally omitted.
"""
